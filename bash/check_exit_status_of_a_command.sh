#!/bin/bash

# ==================================
# = check exit status of a command =
# ==================================

# a command that fails
echo $?

# returns 0 if the command is successful
# returns a non-zero if it fails