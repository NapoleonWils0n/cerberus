#!/bin/bash

 # ===================================
 # = unix remove non-empty directory =
 # ===================================


# the -r means it's recursive, the f suppresses the need to confirm every file deletion.

rm -rf directoryname